<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>07数组排序之选择排序法</title>
	<script>
		/*
			选择排序法(先排小的)
				* 把当前元素分别跟后面的元素对比
				* 把最小的逐个往前排列

			目的：得到一个从小到达排列的数组
		 */
		
		var arr = [10,2,8,55,32,18,9,11,30,16,19,20];

		// 遍历数组

		// 外循环：遍历当前元素
		for(var i=0;i<arr.length-1;i++){

			// 内循环:遍历后面的元素
			for(var j=i+1;j<arr.length;j++){
				// 当前元素分别跟后面的元素进行对比
				if(arr[i] > arr[j]){
					// 交换值
					var temp = arr[i];
					arr[i] = arr[j];
					arr[j] = temp;
				}
			}
		}

		console.log(arr.reverse());
	</script>
</head>
<body>
	<h1>07数组排序之选择排序法</h1>
</body>
</html>